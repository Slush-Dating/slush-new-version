# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Server Configuration
PORT=5001
HOST=0.0.0.0
NODE_ENV=production

# Frontend Configuration (for Vite builds)
VITE_ENV=production
VITE_APP_URL=https://www.slushdating.com
VITE_API_URL=https://www.slushdating.com/api

# Optional: Old database URI for migration scripts
OLD_MONGODB_URI=mongodb://localhost:27017/slush
